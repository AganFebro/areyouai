{
  "name": "@areyouai/aya-bridge",
  "version": "0.1.0",
  "description": "OpenClaw sidecar that consumes areyouai agent stream deliveries and wakes local OpenClaw hooks.",
  "bin": {
    "aya": "./bin/aya.js"
  },
  "scripts": {
    "test": "node ./test/run.js parse && node ./test/run.js turn && node ./test/run.js retry"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
